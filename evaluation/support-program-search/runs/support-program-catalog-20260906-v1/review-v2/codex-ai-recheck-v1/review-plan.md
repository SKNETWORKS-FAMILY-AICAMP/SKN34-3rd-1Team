# 미확정 42건 추가 검토 계획

이 계획은 추가 판정의 결과를 수집하기 전에 고정한다. 이번 범위는 기존 AI-only에서 미확정인 42개
질문·공고 조합의 원인 분석과 한 차례의 추가 AI 판정이다. 기존 합의 279건은 그대로 재사용한다.

- 부모 실행: `../codex-ai-v1/ai-review.json`.
- 부모 파일 SHA-256: `5940264cafa520edafbb058811d24bf6c21f25f92e09d4318391b120d57da2c6`.
- 입력: 부모 실행에서 미확정인 42건 전부. 질문·고정 공고 본문·기준 날짜를 바꾸지 않는다.
- 판정 기준: 부모 `policy.json`의 rubric·judge별 instruction과 동일하다. 본문에 없는 사실은 추정하지 않는다.
- 독립 추가 판정: Codex `gpt-5.6-luna` 작업 5개가 각각 42건을 읽는다. 총 210건, 추가 검토는 1회로 제한한다.
- 각 judge는 다른 판정·기존 득표·사람 판정·검색 순위·예상 정답을 보지 않고 판단한다.
- 합의 기준: 추가 5개 판정이 모두 완료되고, 같은 확정 판정이 4개 이상인 경우만 채택한다.
- 대상 42건은 추가 5개 판정으로 교체한다. 기존 판정과 합쳐 10표로 계산하거나 유리한 표를 고르지 않는다.
- 추가 판정도 미확정이면 그대로 남긴다. 합의될 때까지 반복하거나 기준을 낮추지 않는다.
- 새 판단은 AI 참조 판정이며 사람 검증으로 표시하지 않는다. 같은 모델의 공통 오류는 남을 수 있다.
- 대상 42건에는 dev·heldout 질문이 모두 포함된다. 검색 결과·점수를 확인하기 전 라벨 준비 단계의 검토다.
- 원인 분석은 독립 판정 파일이 확정된 뒤, 별도 파일에서 기존 이유·본문을 대조해 기록한다.
- 프로젝트 OpenAI API 키·웹 검색·실제 검색 캡처를 사용하지 않는다. Codex 작업 사용량은 발생한다.

판정 결과의 변경과 그 출처를 새 `ai-recheck.json` 및 새 선택 결과로 보관한다. 기존 판정 파일·스냅샷은
그대로 남긴다. 추가 결과의 합의 수가 늘어도 실제 검색 품질 점수가 생기는 것은 아니다.
